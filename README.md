Producer: Create a producer that generates intel about persons:
• Use the faker library
• Produce 100 messages in the format:

• Queue: use a regular queue

• Consumer: Create a consumer that fakes writing to DB
